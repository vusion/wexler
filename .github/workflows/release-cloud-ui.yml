name: release
on: [push, workflow_call]
jobs:
  release-cloud-ui:
    runs-on: self-hosted
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: cloud-ui.vusion@0.18.2
      - run: curl 'http://10.219.196.129:1454/github-workflows/release-cloud-ui'
